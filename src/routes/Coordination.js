export const goTologin = (navigate) => {
  navigate('/Login');
};

export const goToRestaurants = (navigate) => {
  navigate('/Restaurant');
};

export const goToProfile = (navigate) => {
  navigate('/Profile');
};
  
export const goToUpdateProfile = (navigate) => {
  navigate('/UpdateProfile');
};
  
export const goToError = (navigate, name) => {
  navigate('/Error');
};